# Phase 5 移行チェックリスト
**作成：2026-05-02　対象時期：2026-07-27（Phase 4終了）以降**

---

## Phase 4 合格判定（2026-07-27）

Phase 4の合格基準を確認し、移行可否を判断する。

- [ ] 3ヶ月連続で月次損失10%以内
- [ ] 模擬年利8%以上（2026-04-27〜07-27）
- [ ] 重大なバグ・システム障害なし

> ⚠️ 合格基準未達の場合はPhase 4を延長（1ヶ月単位）し、再判定する

---

## Step 1：Phase 4 結果分析（〜2週間）

### 1-1. 戦略別パフォーマンス検証
- [ ] `auto_report.py` の月次サマリーを戦略別に集計
- [ ] 戦略A・C・D・E それぞれの実績（勝率・損益・取引数）を確認
- [ ] PDCAログの記録事項を整理し、統計的根拠のある変更案を抽出

### 1-2. 戦略C・D の廃止判断
- [ ] 戦略C：Phase 4実績が20年バックテストと一致するか確認
  - 一致（勝率30〜44%・マイナス傾向）→ 廃止確定
  - 予想外にプラス → 廃止保留・追加検証
- [ ] 戦略D：Phase 4でのシグナル発生数を確認
  - 発生数ゼロまたは極少 → 廃止確定
  - 発生があった場合 → 損益を確認して判断

### 1-3. 保有日数の見直し
- [ ] 20年バックテスト結果：10日と15日が同点（スコア+14.3）
- [ ] Phase 4での強制終了割合を確認（`scheduler_log.txt`）
- [ ] 変更する場合：10日 → 15日（理由：取引コスト削減・同等スコア）
- [ ] 変更不要の場合：現行10日を維持

---

## Step 2：候補銘柄の再選定（〜1週間）

### 2-1. 戦略A v2候補リスト作成
- [x] `oos_backtest.py` のDATA_PATHを `prices_20y.parquet` に更新（2026-05-03）
- [x] 実行時に `logs/final_candidates_v2.csv` を自動保存するよう修正（2026-05-03）
- [ ] `python oos_backtest.py` を実行して v2 候補リスト生成
- [ ] 現行30銘柄と比較して差分を確認

### 2-2. 戦略E v2候補リスト作成
- [x] `oos_backtest_cd.py` のDATA_PATHを `prices_20y.parquet` に更新（2026-05-03）
- [x] 戦略E実行時に `logs/strategy_e_candidates_v2.csv` を自動保存するよう修正（2026-05-03）
- [ ] `python oos_backtest_cd.py E` を実行して v2 候補リスト生成
- [ ] 候補30銘柄と差分を確認

### 2-3. 戦略F 候補スキャナー整備
- [x] `scanner.py` に戦略F（PEAD）のシグナル検出を追加（2026-05-02実装済み）
- [x] `auto_entry.py` に戦略F対応を追加（Strategy='F'・2026-05-03確認済み）
- [x] `auto_exit.py` に戦略F対応を追加（保有15日・2026-05-03確認済み）

### 2-4. 配当落ち日除外フィルター（2026-05-03 新規発見）
> IS期間分析で ExDate前3日以内のシグナルの勝率が35%（ベース68%比 -33pt）、損切率40%と判明

- [ ] `scanner.py` に配当落ち日除外フィルターを追加
  - `dividends.parquet` または J-Quants 配当API から ExDate を取得
  - シグナル日が `ExDate - 3営業日` 以内の場合 Signal=False に抑制
  - 対象戦略: A / E（逆張りCはむしろプラスの可能性があるため別途検討）
- [ ] `analyze_dividends.py` の OOS 期間（2023-2026）でも検証して採用判断

### 2-5. 戦略A/E v2 候補リストの正式採用
- [x] `python oos_backtest_cd.py E` 実行済み → 2593除外漏れで OOS +2.53%・MaxDD -22.23%（失敗）
- [x] `oos_backtest_cd.py` に `"E": {"25930"}` 除外追加（2026-05-03）
- [x] `python oos_backtest_cd.py E` 再実行済み（伊藤園除外後）→ OOS +2.28%・MaxDD -22.68%（v1比悪化）
- [ ] 戦略E の Phase 5採用判断:
  - v1（伊藤園含む）: OOS +7.56%・MaxDD -11.33% ← 現行 strategy_e_candidates.csv
  - v2（伊藤園除外）: OOS +2.28%・MaxDD -22.68% ← 大幅悪化・採用しない
  - **推奨**: Phase 5は v1 継続使用。伊藤園シグナル発生を Phase 5 で3ヶ月観察して判断。
- [ ] 戦略A v2採用（Phase 5移行時）: `final_candidates_v2.csv` → `final_candidates.csv` にリネーム
  - v2: OOS +8.86%・MaxDD -10.15%（v1比 +4.35pt改善、70%銘柄入替）

---

## Step 3：kabu API 実取引設定（〜3日）

### 3-1. kabuステーション® 設定
- [ ] kabuステーション® Professional プランを確認
- [ ] API利用をON（システム設定 → APIタブ）
- [ ] スタートアップ登録（PC再起動後に自動起動）

### 3-2. 接続テスト
- [ ] `python src/broker_client.py` で接続確認
- [ ] 残高照会が正常に動作することを確認

### 3-3. ドライラン（ペーパー + 実注文の並行確認）
- [ ] `.env` の `LIVE_TRADING=false` を維持
- [ ] `broker_client.py` のテスト発注機能で模擬注文が通るか確認
  （kabu APIの「テスト注文」機能を使用）

### 3-4. Phase 5 開始設定
- [ ] `.env` を更新
  ```
  LIVE_TRADING=true
  KABU_TRADE_PASSWORD=（取引パスワード）
  PHASE5_MAX_CAPITAL=500000
  ```
- [ ] 初回実注文前に残高・余力を手動確認

---

## Step 4：Phase 5 運用開始

### 4-1. 少額実運用（上限50万円）
- [ ] 初期投入：50万円
- [ ] 最大5ポジション × 最大10万円/銘柄（Phase 5では20万円から縮小検討）
- [ ] 月次損失上限：運用資金の10%（5万円）

### 4-2. 監視体制
- [ ] ntfy通知の確認（エントリー・決済・エラー）
- [ ] 毎週末にExcelログを手動確認
- [ ] 月末にAuto_reportの月次集計を確認

### 4-3. Phase 6 移行条件
- [ ] Phase 5で3ヶ月連続：月次損失10%以内 かつ 年利換算15%以上
- [ ] 達成後に投入資金を100万円フルに拡大

---

## 参考：Phase 5 戦略構成

| 戦略 | 種類 | 候補 | TP | SL | 保有 |
|------|------|------|----|----|----|
| A | 順張りモメンタム | 30銘柄 | ATR×6 | ATR×2 | 10日 |
| E | 52週高値ブレイク | 30銘柄 | ATR×6 | ATR×2 | 10日 |
| F | 決算モメンタム | 全プライム | ATR×5 | ATR×2 | 15日 |
| ~~C~~ | ~~平均回帰~~ | 廃止 | — | — | — |
| ~~D~~ | ~~ギャップアップ~~ | 廃止または再設計 | — | — | — |

---

*本チェックリストは Phase 4 終了時に最終確認・更新すること*
*Last Updated: 2026-05-02*
